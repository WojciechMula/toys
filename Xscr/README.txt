Simple direct-screen abstraction for X Window. Public domain.

--------------------------------------------------

Library makes abstraction of direct screen access under X Window.
A user supply its own pixel buffer, that is displayed in a window.
Automatic conversion between some pixels formats are builtin.

Library allows to register callbacks that are called on keyboard
or mouse events.
